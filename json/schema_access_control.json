{
  "$defs": {
    "ControllerConnectionEnum": {
      "description": "How a controller communicates with doors/readers.",
      "enum": [
        "LAN",
        "BUS",
        "WIRELESS",
        "HYBRID"
      ],
      "title": "ControllerConnectionEnum",
      "type": "string"
    },
    "ControllerRule": {
      "additionalProperties": false,
      "description": "Defines which DoorRule areas are managed by a specific controller.",
      "properties": {
        "areas": {
          "description": "Physical location(s) where controller hardware is installed",
          "items": {
            "type": "string"
          },
          "title": "Areas",
          "type": "array"
        },
        "manages_door_areas": {
          "description": "DoorRule area names this controller manages",
          "items": {
            "type": "string"
          },
          "title": "Manages Door Areas",
          "type": "array"
        },
        "connection_type": {
          "anyOf": [
            {
              "$ref": "#/$defs/ControllerConnectionEnum"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "How controller communicates with doors/readers"
        },
        "description": {
          "description": "Human-readable summary of the rule",
          "title": "Description",
          "type": "string"
        }
      },
      "required": [
        "description"
      ],
      "title": "ControllerRule",
      "type": "object"
    },
    "DoorCloserEnum": {
      "description": "Type of door closer installed on the door.",
      "enum": [
        "NONE",
        "MECHANICAL",
        "ELECTRIC"
      ],
      "title": "DoorCloserEnum",
      "type": "string"
    },
    "DoorRule": {
      "additionalProperties": false,
      "description": "Defines the access control configuration for one or more doors.",
      "properties": {
        "areas": {
          "description": "Space names this rule applies to. A door matches when either of its adjacent spaces appears in this list (case-insensitive substring).",
          "items": {
            "type": "string"
          },
          "title": "Areas",
          "type": "array"
        },
        "door_id": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Explicit ID of a single door to target. Use only when the requirement identifies one specific door by name or ID. Prefer areas-based rules when multiple doors share the same configuration.",
          "title": "Door Id"
        },
        "connects_to_areas": {
          "description": "When set together with areas, only doors that directly connect a space in areas to a space in connects_to_areas are matched (bidirectional). Use this to target the boundary door between two specific spaces.",
          "items": {
            "type": "string"
          },
          "title": "Connects To Areas",
          "type": "array"
        },
        "reader_direction": {
          "anyOf": [
            {
              "$ref": "#/$defs/ReaderDirectionEnum"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Direction in which readers are installed"
        },
        "reader_types": {
          "description": "Authentication technologies used on the door",
          "items": {
            "$ref": "#/$defs/ReaderTypeEnum"
          },
          "title": "Reader Types",
          "type": "array"
        },
        "lock_type": {
          "anyOf": [
            {
              "$ref": "#/$defs/LockTypeEnum"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Type of lock installed on the door"
        },
        "door_closer": {
          "anyOf": [
            {
              "$ref": "#/$defs/DoorCloserEnum"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Type of door closer installed"
        },
        "exit_device": {
          "anyOf": [
            {
              "$ref": "#/$defs/ExitDeviceEnum"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Exit trigger device on the secure side"
        },
        "emergency_button": {
          "anyOf": [
            {
              "$ref": "#/$defs/EmergencyButtonEnum"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Emergency release device on the non-secure side"
        },
        "exit_device_side_hint": {
          "anyOf": [
            {
              "$ref": "#/$defs/PlacementSideHintEnum"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional placement hint for exit devices. Use this only when the source text explicitly states the side."
        },
        "emergency_button_side_hint": {
          "anyOf": [
            {
              "$ref": "#/$defs/PlacementSideHintEnum"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional placement hint for emergency buttons. Use this only when the source text explicitly states the side."
        },
        "door_sensor": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Indicates whether the door has an open/close sensor",
          "title": "Door Sensor"
        },
        "anti_passback": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Whether anti-passback logic is enabled",
          "title": "Anti Passback"
        },
        "description": {
          "description": "Human-readable summary of the rule",
          "title": "Description",
          "type": "string"
        }
      },
      "required": [
        "description"
      ],
      "title": "DoorRule",
      "type": "object"
    },
    "EmergencyButtonEnum": {
      "description": "Emergency release device for the door.",
      "enum": [
        "NONE",
        "STANDALONE",
        "INTRUSION"
      ],
      "title": "EmergencyButtonEnum",
      "type": "string"
    },
    "ExitDeviceEnum": {
      "description": "Device used to trigger exit from the secure side.",
      "enum": [
        "NONE",
        "PUSH_BUTTON",
        "ELBOW_ACTUATED",
        "GROUND_LOOP",
        "MOTION_SENSOR"
      ],
      "title": "ExitDeviceEnum",
      "type": "string"
    },
    "LockTypeEnum": {
      "description": "Type of access control lock installed on the door.",
      "enum": [
        "SOLENOID_LOCK",
        "ELECTRIC_STRIKE_PLATE",
        "MAGNETIC_LOCK",
        "MOTORIZED_LOCK",
        "OTHER",
        "NONE"
      ],
      "title": "LockTypeEnum",
      "type": "string"
    },
    "PlacementSideHintEnum": {
      "description": "Optional relative-side hint for projected door accessories.",
      "enum": [
        "UNSPECIFIED",
        "SAME_AS_READER",
        "OPPOSITE_AS_READER",
        "BOTH"
      ],
      "title": "PlacementSideHintEnum",
      "type": "string"
    },
    "ReaderDirectionEnum": {
      "description": "Direction in which authentication readers are installed on a door.",
      "enum": [
        "IN_ONLY",
        "OUT_ONLY",
        "IN_OUT",
        "NONE"
      ],
      "title": "ReaderDirectionEnum",
      "type": "string"
    },
    "ReaderTypeEnum": {
      "description": "Authentication technology used by the reader device.",
      "enum": [
        "CARD",
        "CODE",
        "FINGERPRINT",
        "FACE_RECOGNITION",
        "IRIS_SCAN",
        "BLUETOOTH",
        "NFC",
        "WIRELESS_KEYFOB",
        "MOBILE_APP",
        "LICENSEPLATE_CAMERA",
        "INTERCOM",
        "OTHER"
      ],
      "title": "ReaderTypeEnum",
      "type": "string"
    },
    "Requirements": {
      "additionalProperties": false,
      "properties": {
        "door_rules": {
          "items": {
            "$ref": "#/$defs/DoorRule"
          },
          "title": "Door Rules",
          "type": "array"
        },
        "controller_rules": {
          "items": {
            "$ref": "#/$defs/ControllerRule"
          },
          "title": "Controller Rules",
          "type": "array"
        }
      },
      "title": "Requirements",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "properties": {
    "system_type": {
      "const": "ACCESS_CONTROL",
      "default": "ACCESS_CONTROL",
      "title": "System Type",
      "type": "string"
    },
    "version": {
      "default": "1.0",
      "title": "Version",
      "type": "string"
    },
    "requirements": {
      "$ref": "#/$defs/Requirements"
    }
  },
  "required": [
    "requirements"
  ],
  "title": "AccessControlSchema",
  "type": "object"
}